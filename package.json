{
  "name": "backpack-capital-composer",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "python3 -m http.server 4173",
    "build": "mkdir -p dist && cp index.html style.css app.js dist/ && cp -R public dist/public"
  }
}
